/*
mathx is an addition to the std math pkg. It contains some helper methods
but primarily has distance funcs and a type representing a numeric vec which
wraps a []float64 in a read-only way such that it is thread-safe.
*/
package mathx
